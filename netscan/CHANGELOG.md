# Changelog

## Unreleased

- Added Ingress support: NetScan now appears as an admin-only panel in the Home Assistant
  sidebar instead of requiring a direct port. The `port` option was removed (fixed at 8099
  internally, matching `ingress_port`).

## 1.0.5

- Initial Home Assistant add-on release, tracking the main project's `netscan` Docker image.
